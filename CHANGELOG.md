# 更新日志（Changelog）

## v1.1.5（2026-08-22）

### 修复
- **用量/花费统计虚高（与 DeepSeek 后台账单核对后修复）**：dsh 开新会话时会把父会话历史整段复制进新日志，同一笔 API 请求最多被重复记录 9 次，导致今日请求数虚胖 4 倍多、花费估算虚高（实测 ¥105.50 → 实际 ¥27.76）。现按「时间戳 + 三段 tokens」签名**跨会话去重**，每笔只计一次
- **只统计 DeepSeek 直连请求**：走 OpenRouter 等第三方渠道的调用不扣 DeepSeek 余额，不再计入用量与花费（按日志中 `source.provider` 过滤）
- 累计用量改为与今日同源（扫描全部日志去重统计），弃用 `session_projcache.json` 缓存（该缓存含重复记录且不分渠道，数字虚高）

### 说明
- 修复后本机估算与官方后台账单基本一致：实测同日后台消费 ¥28.44 / 733 次 / 160.5M tokens，本机估算 ¥27.76 / 696 次 / 156.9M tokens（小额差额来自同一密钥下其他程序的用量，本机日志无法覆盖）
- 高峰/空闲分时段计价逻辑经核对**无误**，保持不变

## v1.1.4（2026-08-17）

### 调整
- 关于弹窗改为**居中布局**：图标、名称、版本（v1.1.4）、用途、作者（@VincentPhoton）全部居中
- 检查更新弹窗的图标同样居中（自定义居中弹窗替代 NSAlert）

## v1.1.3（2026-08-17）

### 新增
- **菜单栏右键快捷菜单**：刷新 / 检查更新 / 关于（名字、版本、用途、作者）/ GitHub 主页 / 退出

## v1.1.2（2026-08-17）

### 新增
- **刷新频率可选**：3 / 5 / 10 分钟三档（默认 5 分钟），修改即时生效并记住选择

## v1.1.1（2026-08-17）

### 调整
- 版本号规范为三位（x.y.z），后续小改动按 0.0.1 步进
- 面板版本号改为**蓝色 + 下划线**（明确提示可点击，跳转 GitHub 主页）
- 检查更新结果改为**屏幕中央弹窗**：显示当前版本与上次检查更新时间，点"好的"关闭

## v1.1（2026-08-17）

### 新增功能
- **高峰前提醒**：可设置提前 0–60 分钟，进入高峰前发系统通知（仅空闲时段提醒，同一波高峰只提醒一次）
- **提醒时间设置**：滑块 + 数字框双向联动（拖滑块改数字、改数字滑块跟随，0 = 高峰开始时）
- **时段状态显示**：面板显示当前是高峰还是空闲时段（含时间段），并倒计时距当前时段结束还有多久；颜色区分（高峰红 / 空闲绿），空闲时段剩 ≤30 分钟时倒计时变红警示
- **高峰/空闲用量分开统计**：今日用量分别显示高峰时段与空闲时段的 token 用量
- **花费精确计价**：按每次请求的实际时间计价——2026-08-17 前按老价；之后高峰时段（北京时间 9:00–12:00、14:00–18:00）按高峰价，其余按空闲价
- **面板内粘贴 API 密钥**：首次使用可直接粘贴密钥（存入 Mac 钥匙串，无需配置文件；dsh 用户不受影响）
- **版本号 + 检查更新**：面板显示当前版本号（点击跳转 GitHub 主页），一键检查更新——发现新版自动下载安装并重启（依次显示"正在下载更新 → 正在安装更新 → 更新已完成"）

### 修复与优化
- 修正高峰时段定义（北京时间 9:00–12:00、14:00–18:00）
- 面板交互细节优化（数字框焦点管理、布局调整）

### 兼容性
- zstd 路径自动探测（兼容 Apple Silicon / Intel Homebrew 等常见安装位置）

## v1.0（2026-08-16）

### 初始版本
- macOS 菜单栏常驻显示 DeepSeek API 余额（可开关，每 10 分钟自动刷新）
- 下拉面板：余额 / 充值 / 赠送、今日用量、累计用量、花费估算
- 实心背景高对比度，深色 / 浅色模式自适应
- 附一键卸载程序（自动清理偏好与缓存，不影响 dsh 数据）
- 自检模式：`SeekBalance --once`
